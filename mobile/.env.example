# Only needed if auto-detection (via Metro's dev host) doesn't work for your
# setup — e.g. a standalone/production build, or a physical device that
# can't reach the dev machine's inferred LAN address. Copy this file to
# .env and uncomment/edit:
# EXPO_PUBLIC_API_BASE_URL=http://192.168.1.50:8000
